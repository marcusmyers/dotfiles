-- Personal keyboard overrides loaded after Omarchy's defaults.
hl.config({
  input = {
    -- Make Caps Lock an additional Control key; keep the original Control key.
    kb_options = "ctrl:nocaps",
  },
})
